<UserControl x:Class="GestorPDV.Wpf.Views.DbStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="GestorPDV" FontSize="28" FontWeight="Bold" Foreground="#1F2937" />
            <TextBlock Text="ERP/PDV — varejo, oficina, supermercado, farmácia, indústria e segmentos similares"
                       FontSize="13" Foreground="#6B7280" Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,24,0,0" Padding="16" CornerRadius="6"
                Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="12" Height="12" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#F59E0B" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Falhou}" Value="True">
                                    <Setter Property="Fill" Value="#DC2626" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding Mensagem}" TextWrapping="Wrap" VerticalAlignment="Center" MaxWidth="600" />
            </StackPanel>
        </Border>

        <Button Grid.Row="2" Margin="0,16,0,0" Padding="12,6" HorizontalAlignment="Left"
                Content="Tentar novamente" Command="{Binding TentarNovamenteCommand}"
                Visibility="{Binding Falhou, Converter={StaticResource BoolParaVisibilidade}}" />
    </Grid>
</UserControl>
