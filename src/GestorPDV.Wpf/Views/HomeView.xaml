<UserControl x:Class="GestorPDV.Wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#1F2937">
                    <Run Text="Bem-vindo, " /><Run Text="{Binding Sessao.Nome, Mode=OneWay}" />
                </TextBlock>
                <TextBlock FontSize="12" Foreground="#6B7280" Margin="0,4,0,0">
                    <Run Text="Usuário: " /><Run Text="{Binding Sessao.Login, Mode=OneWay}" />
                </TextBlock>
            </StackPanel>

            <Button Grid.Column="1" Content="Sair" Command="{Binding SairCommand}"
                    Padding="12,6" VerticalAlignment="Top" />
        </Grid>

        <Border Grid.Row="1" Margin="0,24,0,0" Padding="16" CornerRadius="6"
                Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Permissões do usuário" FontWeight="Bold" Foreground="#1F2937" />
                <TextBlock Text="{Binding PermissoesTexto}" TextWrapping="Wrap" Margin="0,6,0,0"
                           Foreground="#374151" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Margin="0,24,0,0" VerticalAlignment="Bottom"
                   Foreground="#9CA3AF" FontSize="12" TextWrapping="Wrap"
                   Text="Fase 4 concluída: login, permissões e troca de senha. Cadastros, vendas, pagamentos, relatórios e impressão chegam nas próximas fases." />
    </Grid>
</UserControl>
